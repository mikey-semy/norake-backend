# Руководство по настройке MCP Plane Integration

## Содержание

1. [Обзор](#обзор)
2. [Предварительные требования](#предварительные-требования)
3. [Настройка для Claude Desktop](#настройка-для-claude-desktop)
4. [Настройка для VS Code](#настройка-для-vs-code)
5. [Проверка подключения](#проверка-подключения)
6. [Использование](#использование)
7. [Устранение неполадок](#устранение-неполадок)

---

## Обзор

MCP Plane Integration позволяет AI ассистентам (Claude, GitHub Copilot) напрямую работать с задачами в Plane:

- ✅ Получать информацию о задачах
- ✅ Обновлять статусы задач
- ✅ Добавлять комментарии
- ✅ Получать списки задач и проектов
- ✅ Работать с состояниями (states)

---

## Предварительные требования

### 1. Получить API ключ Plane

1. Войдите в Plane: https://plane.equiply.ru/
2. Перейдите в **Settings → API Tokens**
3. Создайте новый токен с правами на чтение и запись
4. Сохраните токен в безопасном месте

### 2. Установить Node.js

MCP сервер Plane работает через Node.js:

```bash
# Проверить установку
node --version  # должно быть >= 16.x

# Если не установлен, установить с https://nodejs.org/
```

---

## Настройка для Claude Desktop

### Шаг 1: Найти конфигурационный файл

**macOS/Linux:**
```bash
~/.config/claude/claude_desktop_config.json
```

**Windows:**
```
%APPDATA%\Claude\claude_desktop_config.json
```

### Шаг 2: Добавить MCP сервер Plane

Откройте файл и добавьте в секцию `mcpServers`:

```json
{
  "mcpServers": {
    "plane": {
      "command": "npx",
      "args": ["-y", "@makeplane/plane-mcp-server"],
      "env": {
        "PLANE_API_KEY": "ВАШ_API_КЛЮЧ_ЗДЕСЬ",
        "PLANE_API_HOST_URL": "https://plane.equiply.ru/",
        "PLANE_WORKSPACE_SLUG": "projects"
      }
    }
  }
}
```

**⚠️ ВАЖНО**: Замените `ВАШ_API_КЛЮЧ_ЗДЕСЬ` на реальный API ключ!

### Шаг 3: Перезапустить Claude Desktop

После сохранения конфигурации полностью закройте и откройте заново Claude Desktop.

---

## Настройка для VS Code

### Шаг 1: Установить расширение

Убедитесь, что установлено расширение для MCP в VS Code (если доступно).

### Шаг 2: Создать конфигурационный файл

В корне проекта создайте файл `.vscode/mcp.json`:

```bash
cd /путь/к/equiply-backend
cp .vscode/mcp.json.example .vscode/mcp.json
```

### Шаг 3: Настроить API ключ

Откройте `.vscode/mcp.json` и замените `YOUR_PLANE_API_KEY`:

```json
{
    "servers": {
        "plane": {
            "command": "npx",
            "args": ["-y", "@makeplane/plane-mcp-server"],
            "env": {
                "PLANE_API_KEY": "ВАШ_API_КЛЮЧ_ЗДЕСЬ",
                "PLANE_API_HOST_URL": "https://plane.equiply.ru/",
                "PLANE_WORKSPACE_SLUG": "projects"
            }
        }
    }
}
```

### Шаг 4: Добавить в .gitignore

⚠️ **КРИТИЧНО**: Не коммитьте реальный API ключ в Git!

Убедитесь, что в `.gitignore` есть:

```gitignore
# MCP конфигурация с секретами
.vscode/mcp.json
```

---

## Проверка подключения

### Метод 1: Через Python скрипт

```bash
# Установить переменную окружения
export PLANE_API_KEY="ваш_api_ключ"

# Запустить проверку
python scripts/check_plane_api.py
```

Ожидаемый вывод при успешном подключении:

```
🚀 ПРОВЕРКА PLANE API
============================================================
Host: https://plane.equiply.ru
Workspace: projects
API Key: ✅ Установлен

🔍 Проверка подключения к https://plane.equiply.ru...
   ✅ Базовое подключение успешно

🔍 Проверка workspace 'projects'...
   ✅ Workspace найден: Projects

🔍 Получение списка проектов в workspace 'projects'...
   ✅ Найдено проектов: X
      - Equiply Backend (NORAK)

...

🎉 Все проверки пройдены успешно!
```

### Метод 2: Через Claude Desktop

1. Откройте Claude Desktop
2. В нижней части окна должна появиться иконка 🔌 (MCP серверы)
3. Кликните на иконку - должен отображаться сервер "plane"
4. Попросите Claude: "Получи список задач из проекта NORAK"

---

## Использование

### Базовые команды в промптах

#### Получить задачу по ID

```
Получи информацию о задаче NORAK-1
```

Внутренне будет вызван:
```python
mcp_plane_get_issue_using_readable_identifier(
    project_identifier="NORAK",
    issue_identifier="1"
)
```

#### Список задач проекта

```
Покажи все задачи проекта Equiply Backend
```

#### Обновить статус задачи

```
Обнови статус задачи NORAK-5 на "In Progress"
```

#### Добавить комментарий

```
Добавь комментарий к задаче NORAK-3: "Реализовано согласно требованиям"
```

### Статусы проекта (State IDs)

```python
STATES = {
    "backlog": "ee55ae62-d94b-4236-8044-bf51830e89be",
    "todo": "a2bf76ee-e462-4e3b-aba8-732b7133e13a",
    "in_progress": "6e814f64-8acd-4b61-992d-b89ebf6bf55e",
    "done": "749a5e1b-a62a-4b50-964b-816ffe1f4dad",
    "cancelled": "a292224e-8a52-4835-81c1-9e7b36743166"
}
```

### Workflow для разработки

См. подробное описание в [copilot-instructions.md](../.github/copilot-instructions.md):

1. **Перед началом работы** - получить задачу из Plane
2. **Создать feature ветку** - `feature/NORAK-XX-description`
3. **Обновить статус** - на "In Progress"
4. **Разработка** - следовать требованиям из задачи
5. **Завершение** - обновить статус на "Done", добавить комментарий

---

## Устранение неполадок

### Проблема: MCP сервер не запускается

**Решение 1**: Проверить установку Node.js
```bash
node --version  # должно быть >= 16
npm --version
```

**Решение 2**: Установить MCP сервер вручную
```bash
npx -y @makeplane/plane-mcp-server
```

**Решение 3**: Проверить логи Claude Desktop
- macOS: `~/Library/Logs/Claude/`
- Windows: `%APPDATA%\Claude\logs\`

### Проблема: 401 Unauthorized

**Причины**:
- Неверный API ключ
- API ключ истёк
- API ключ не имеет необходимых прав

**Решение**:
1. Проверить API ключ в конфигурации
2. Создать новый токен в Plane
3. Убедиться, что токен имеет права на чтение и запись

### Проблема: 404 Not Found для workspace

**Причина**: Неверный `PLANE_WORKSPACE_SLUG`

**Решение**:
Убедитесь, что в конфигурации используется:
```json
"PLANE_WORKSPACE_SLUG": "projects"
```

А НЕ `"equiply"` или другие значения!

### Проблема: Скрипт не может подключиться к plane.equiply.ru

**Причина**: Домен может быть недоступен из некоторых сетей

**Решение**:
1. Проверить VPN/прокси настройки
2. Проверить доступность через браузер: https://plane.equiply.ru/
3. Проверить DNS: `nslookup plane.equiply.ru`

---

## Дополнительная информация

### Ссылки

- **Документация Plane API**: https://docs.plane.so/
- **MCP Plane Server**: https://github.com/makeplane/plane-mcp-server
- **Model Context Protocol**: https://modelcontextprotocol.io/

### Конфигурация проекта

- **Project ID**: `c4ea1c3f-97d2-4f56-8aaa-5cce4b185f58`
- **Project Identifier**: `NORAK`
- **Workspace**: `projects`
- **Plane URL**: `https://plane.equiply.ru/`

### Безопасность

⚠️ **НИКОГДА**:
- Не коммитьте API ключи в Git
- Не делитесь API ключами в публичных каналах
- Не используйте production ключи для разработки

✅ **ВСЕГДА**:
- Храните ключи в переменных окружения
- Используйте `.gitignore` для `mcp.json`
- Создавайте отдельные токены для разных окружений
- Регулярно ротируйте API ключи

---

## Поддержка

При проблемах с настройкой:

1. Проверьте [MCP_PLANE_CHECK_RESULTS.md](./MCP_PLANE_CHECK_RESULTS.md)
2. Запустите `python scripts/check_plane_api.py`
3. Создайте issue в репозитории с подробным описанием проблемы

---

**Последнее обновление**: 2025-11-13
**Версия документа**: 1.0
