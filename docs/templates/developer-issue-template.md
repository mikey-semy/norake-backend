# Шаблон для программистов: Запрос помощи

## 📋 Общие рекомендации

**Цель шаблона**: Помочь структурировать запрос помощи так, чтобы получить быстрый и точный ответ.

**Статусы**:
- 🔴 **RED** (Проблема) - требуется помощь, проблема заполнена по шаблону
- 🟢 **GREEN** (Решено) - проблема решена, solution заполнено

**Принцип**: Все проблемы остаются в истории с решениями для всех.

---

## 🎯 Шаблон Issue

### 1. Цель / Что нужно достичь

**Поле**: `goal` (text, required)

**Описание**: Опишите конечную цель, а не просто "не работает".

**Примеры**:
- ✅ "Нужно интегрировать OAuth2 авторизацию через Google в FastAPI"
- ✅ "Необходимо оптимизировать SQL-запрос, который выполняется >5 секунд"
- ❌ "Код не работает"
- ❌ "Ошибка"

---

### 2. Проблема / Что происходит сейчас

**Поле**: `current_behavior` (text, required)

**Описание**: Четкое описание текущего поведения системы.

**Примеры**:
- "При попытке логина через Google возвращается HTTP 401 Unauthorized"
- "Запрос к базе данных возвращает пустой результат, хотя данные есть"

---

### 3. Минимальный воспроизводимый пример (MRE)

**Поле**: `code_example` (text, required)

**Описание**: Минимальный код для воспроизведения проблемы. Без секретов, паролей, лишних зависимостей.

**Требования**:
- Убрать всё лишнее (только код, участвующий в ошибке)
- Код должен быть запускаемым (или максимально близким к этому)
- Использовать заглушки для секретных данных

**Пример**:
```python
from fastapi import FastAPI, Depends
from fastapi.security import OAuth2AuthorizationCodeBearer

app = FastAPI()
oauth2_scheme = OAuth2AuthorizationCodeBearer(
    authorizationUrl="https://accounts.google.com/o/oauth2/auth",
    tokenUrl="https://oauth2.googleapis.com/token"
)

@app.get("/login")
async def login(token: str = Depends(oauth2_scheme)):
    # Проблема: всегда возвращает 401
    return {"token": token}
```

---

### 4. Полная ошибка / Traceback

**Поле**: `error_message` (text, required if error exists)

**Описание**: Точный текст ошибки, traceback, логи. **НЕ** скриншот (только если текст недоступен).

**Пример**:
```
Traceback (most recent call last):
  File "main.py", line 12, in login
    return {"token": token}
fastapi.exceptions.HTTPException: 401 Unauthorized
  Detail: Not authenticated
```

---

### 5. Окружение

**Поле**: `environment` (text, required)

**Описание**: Версии языка, библиотек, ОС.

**Структура**:
- **Язык**: Python 3.11.5
- **Фреймворк**: FastAPI 0.104.1
- **ОС**: Ubuntu 22.04 LTS
- **Зависимости**:
  - `httpx==0.25.0`
  - `sqlalchemy==2.0.23`

**Шаблон для быстрого заполнения**:
```bash
# Python
python --version
pip freeze | grep -E "(fastapi|sqlalchemy|pydantic)"

# Node.js
node --version
npm list --depth=0

# System
uname -a  # Linux/macOS
systeminfo  # Windows
```

---

### 6. Что уже пробовали

**Поле**: `attempts` (text, required)

**Описание**: Перечислите, что уже пытались сделать для решения проблемы.

**Примеры**:
- "Читал документацию FastAPI OAuth2: https://fastapi.tiangolo.com/tutorial/security/oauth2-jwt/"
- "Пробовал изменить `authorizationUrl` на `/authorize` (не помогло)"
- "Stack Overflow: https://stackoverflow.com/q/12345678 (не подошло из-за другой версии)"
- "Добавил debug-логирование: токен приходит пустым"

---

### 7. Ожидаемое поведение

**Поле**: `expected_behavior` (text, optional)

**Описание**: Что должно происходить в идеале.

**Пример**:
- "После успешной авторизации в Google должен вернуться `access_token` и пользователь должен быть залогинен"

---

### 8. Дополнительный контекст

**Поле**: `additional_context` (text, optional)

**Описание**: Любая другая полезная информация.

**Примеры**:
- "Проблема появилась после обновления FastAPI с 0.100.0 до 0.104.1"
- "На локальной машине работает, на prod-сервере - нет"
- "При использовании Postman возвращается 200 OK, в браузере - 401"

---

## ✅ Чек-лист перед отправкой

Перед тем как создать Issue, убедитесь:

- [ ] **Попытался решить сам** (документация, Google, Stack Overflow)
- [ ] **Проблема воспроизводится стабильно** (не случайная ошибка)
- [ ] **Код минимизирован** (убрал всё лишнее)
- [ ] **Ошибка полная** (весь traceback, не обрезанный)
- [ ] **Окружение указано** (язык, версии, ОС)
- [ ] **Попытки решения описаны** (что пробовал)
- [ ] **Формулировка вежливая** ("Пожалуйста, помогите" вместо "Срочно!")

---

## 🚫 Что НЕ делать

❌ **Не пишите "не работает"** - это не информативно  
❌ **Не дублируйте вопрос везде** - дождитесь ответа, потом уточните  
❌ **Не прикладывайте 1000 строк кода** - минимизируйте до 10-50 строк  
❌ **Не скрывайте ошибку** - "выдаёт ошибку" → покажите полный текст  
❌ **Не игнорируйте окружение** - версии ВАЖНЫ  

---

## 📍 Где спрашивать

### По приоритету:

1. **Stack Overflow** - для конкретных технических вопросов (тегируйте правильно)
2. **GitHub Issues** - если проблема в конкретной библиотеке/фреймворке
3. **Reddit** (r/learnprogramming, r/Python, r/FastAPI) - для обсуждений
4. **Telegram/Slack/Discord** - тематические чаты (быстрые ответы, но меньше глубины)
5. **NoRake** - внутренняя база знаний команды (остаётся в истории)

---

## 🎯 Пример идеального Issue

**Заголовок**: `FastAPI OAuth2: Google authorization возвращает 401 при редиректе`

**Goal**:  
Интегрировать OAuth2 авторизацию через Google в FastAPI для авторизации пользователей.

**Current Behavior**:  
При переходе на `/login` редирект на Google работает, но после возврата на `/callback` возвращается HTTP 401 Unauthorized с ошибкой "Not authenticated".

**Code Example**:
```python
# main.py
from fastapi import FastAPI, Depends
from fastapi.security import OAuth2AuthorizationCodeBearer

oauth2_scheme = OAuth2AuthorizationCodeBearer(
    authorizationUrl="https://accounts.google.com/o/oauth2/auth",
    tokenUrl="https://oauth2.googleapis.com/token"
)

@app.get("/callback")
async def callback(token: str = Depends(oauth2_scheme)):
    return {"token": token}  # Всегда пустой токен
```

**Error Message**:
```
fastapi.exceptions.HTTPException: 401: Not authenticated
```

**Environment**:
- Python 3.11.5
- FastAPI 0.104.1
- httpx 0.25.0
- Ubuntu 22.04

**Attempts**:
1. Читал документацию: https://fastapi.tiangolo.com/tutorial/security/oauth2-jwt/
2. Пробовал изменить `tokenUrl` на полный URL (не помогло)
3. Stack Overflow: https://stackoverflow.com/q/75000000 (не подошло - другая библиотека)
4. Добавил логирование: `request.headers` показывает, что Authorization header пустой

**Expected Behavior**:  
После редиректа с Google должен вернуться `access_token`, который можно использовать для получения данных пользователя.

**Additional Context**:  
Проблема появилась после миграции с `authlib` на встроенный FastAPI OAuth2. В Postman с ручным токеном работает.

---

## 📖 Дополнительные ресурсы

- [Stack Overflow: How to Ask](https://stackoverflow.com/help/how-to-ask)
- [GitHub: Creating a Minimal Reproducible Example](https://stackoverflow.com/help/minimal-reproducible-example)
- [FastAPI Security Tutorial](https://fastapi.tiangolo.com/tutorial/security/)
- [Python Debugging Guide](https://docs.python.org/3/library/pdb.html)

---

## 🔄 Workflow в NoRake

1. **Создание Issue** (статус RED)
   - Заполните все обязательные поля по шаблону
   - Issue автоматически категоризируется через n8n workflow
   
2. **Поиск решения**
   - Система автоматически ищет похожие Issues в базе знаний
   - Уведомления отправляются команде (опционально)
   
3. **Получение помощи**
   - Коллеги могут предложить решение через комментарии
   - Можно добавить дополнительный контекст/логи
   
4. **Закрытие Issue** (статус GREEN)
   - Укажите решение в поле `solution`
   - Issue остаётся в истории для всех
   - Можно использовать как референс для будущих проблем

---

## 💡 Почему это работает

1. **Структура** - помогает самому понять проблему
2. **Контекст** - дает всю необходимую информацию для помощи
3. **История** - проблемы и решения остаются доступны всем
4. **Уважение** - вежливая формулировка повышает шансы на помощь
5. **Эффективность** - минимизация кода экономит время всех

**Помни**: Хорошо сформулированный вопрос - это 50% ответа!
