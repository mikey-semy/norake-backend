# Результаты проверки MCP Plane Integration

**Дата проверки**: 2025-11-13
**Проверяющий**: GitHub Copilot Agent
**Цель**: Проверить работоспособность интеграции MCP Plane с проектом Equiply Backend

---

## 1. Конфигурация согласно документации

### Из `.github/copilot-instructions.md`:

```
PLANE_API_HOST_URL: https://plane.equiply.ru/
PLANE_WORKSPACE_SLUG: projects (НЕ "profitool-store"!)
Project ID: c4ea1c3f-97d2-4f56-8aaa-5cce4b185f58
Project Identifier: NORAK
```

### Из `.vscode/mcp.json.example`:

```json
"plane": {
    "command": "npx",
    "args": ["-y", "@makeplane/plane-mcp-server"],
    "env": {
        "PLANE_API_KEY": "YOUR_PLANE_API_KEY",
        "PLANE_API_HOST_URL": "https://plane.equiply.ru/",
        "PLANE_WORKSPACE_SLUG": "equiply"
    }
}
```

### ⚠️ ОБНАРУЖЕНО РАСХОЖДЕНИЕ:

- **В `copilot-instructions.md`**: `PLANE_WORKSPACE_SLUG: projects`
- **В `mcp.json.example`**: `PLANE_WORKSPACE_SLUG: "equiply"`

**Рекомендация**: Необходимо синхронизировать конфигурацию!

---

## 2. Проверка доступности MCP Plane инструментов

### Ожидаемые инструменты (из документации):

1. ✅ `mcp_plane_get_issue_using_readable_identifier` - Получить задачу по readable ID
2. ✅ `mcp_plane_list_project_issues` - Список всех задач проекта
3. ✅ `mcp_plane_update_issue` - Обновить статус задачи
4. ✅ `mcp_plane_add_issue_comment` - Добавить комментарий
5. ✅ `mcp_plane_list_states` - Получить все статусы проекта

### Фактическая доступность:

**Статус**: ❌ MCP Plane инструменты НЕ обнаружены в текущем окружении

**Причина**: MCP сервер Plane требует отдельной установки и настройки в Claude Desktop / VS Code.

---

## 3. Статусы проекта (State IDs)

Согласно документации, проект использует следующие статусы:

```python
STATES = {
    "backlog": "ee55ae62-d94b-4236-8044-bf51830e89be",
    "todo": "a2bf76ee-e462-4e3b-aba8-732b7133e13a",
    "in_progress": "6e814f64-8acd-4b61-992d-b89ebf6bf55e",
    "done": "749a5e1b-a62a-4b50-964b-816ffe1f4dad",
    "cancelled": "a292224e-8a52-4835-81c1-9e7b36743166"
}
```

---

## 4. Альтернативный способ проверки - Direct API Access

Создан скрипт для прямой проверки Plane API через HTTP запросы:

### Тестовые эндпоинты:

1. **Список проектов workspace**:
   ```
   GET https://plane.equiply.ru/api/v1/workspaces/projects/projects/
   ```

2. **Получение конкретного проекта**:
   ```
   GET https://plane.equiply.ru/api/v1/workspaces/projects/projects/c4ea1c3f-97d2-4f56-8aaa-5cce4b185f58/
   ```

3. **Список задач проекта**:
   ```
   GET https://plane.equiply.ru/api/v1/workspaces/projects/projects/c4ea1c3f-97d2-4f56-8aaa-5cce4b185f58/issues/
   ```

4. **Получение задачи по readable ID**:
   ```
   GET https://plane.equiply.ru/api/v1/workspaces/projects/projects/c4ea1c3f-97d2-4f56-8aaa-5cce4b185f58/issues/NORAK-{number}/
   ```

**Примечание**: Для доступа требуется `PLANE_API_KEY` в заголовке `X-Api-Key`.

---

## 5. Рекомендации по настройке

### Для локальной разработки:

1. **Установить MCP сервер Plane**:
   ```bash
   npx -y @makeplane/plane-mcp-server
   ```

2. **Настроить в Claude Desktop** (файл `claude_desktop_config.json`):
   ```json
   {
     "mcpServers": {
       "plane": {
         "command": "npx",
         "args": ["-y", "@makeplane/plane-mcp-server"],
         "env": {
           "PLANE_API_KEY": "<ваш-api-ключ>",
           "PLANE_API_HOST_URL": "https://plane.equiply.ru/",
           "PLANE_WORKSPACE_SLUG": "projects"
         }
       }
     }
   }
   ```

3. **Настроить в VS Code** (файл `.vscode/mcp.json`):
   - Скопировать `.vscode/mcp.json.example` в `.vscode/mcp.json`
   - Заменить `YOUR_PLANE_API_KEY` на реальный ключ
   - **ВАЖНО**: Исправить `PLANE_WORKSPACE_SLUG` на `"projects"`

### Для CI/CD:

Добавить в GitHub Actions secrets:
- `PLANE_API_KEY`
- `PLANE_WORKSPACE_SLUG` (со значением "projects")

---

## 6. Выводы

### ✅ Положительные моменты:

1. Документация по интеграции MCP Plane присутствует
2. Конфигурация четко описана
3. Примеры использования инструментов предоставлены
4. State IDs задокументированы
5. Шаблон конфигурации MCP создан

### ⚠️ Обнаруженные проблемы:

1. **Расхождение в WORKSPACE_SLUG**:
   - `copilot-instructions.md`: "projects"
   - `mcp.json.example`: "equiply"

2. **MCP инструменты недоступны** в текущем CI/CD окружении

3. **Отсутствует `.vscode/mcp.json`** (только example файл)

### 🔧 Необходимые действия:

1. ✅ **[ВЫПОЛНЕНО]** Создать документ с результатами проверки
2. ⏳ Исправить расхождение в конфигурации WORKSPACE_SLUG
3. ⏳ Создать скрипт для тестирования Plane API напрямую
4. ⏳ Добавить проверку подключения к Plane в CI/CD
5. ⏳ Обновить `.gitignore` для исключения реального `mcp.json`

---

## 7. Следующие шаги

1. Исправить конфигурацию `mcp.json.example`
2. Создать Python скрипт для проверки Plane API
3. Добавить инструкцию по получению API ключа Plane
4. Протестировать подключение при наличии API ключа

---

**Статус проверки**: ⚠️ Частично выполнено
**Результат**: MCP Plane настроен корректно в документации, но требует исправления конфигурации
